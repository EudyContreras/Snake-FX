package application;

public enum GameStateID {

	MenuState(), 
	GamePlayState(),  
	OptionsMenuState(), 
	InfoScreenState(), 
	AudioOptionsMenuState(), 
	DifficultyMenuState(), 
	ChooseLevelMenuState(), 
	CustomizationMenuState();
}
