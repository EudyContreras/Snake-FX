package com.SnakeGame.IDenums;

/**
 * Keeps a database of the IDs used by different sorts of movements that can be
 * performed by the player or enemy entity
 *
 * @author Eudy Contreras
 *
 */
public enum GameStateID {

	GAME_OVER(), LEVEL_COMPLETED(), GAME_MENU(), MAIN_MENU(), LEADER_BOARD(), GAMEPLAY(), LEVEL_TRANSITIONING(), LEVEL_RESTART(),

}
