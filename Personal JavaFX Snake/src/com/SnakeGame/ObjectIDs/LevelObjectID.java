package com.SnakeGame.ObjectIDs;

/**
 * Keeps a database of the IDs used by different sorts of debris that can be
 * created by the game.
 * 
 * @author Eudy Contreras
 *
 */
public enum LevelObjectID {
	LevelBox(), LevelTile(), LevelTrap(), rock(), fence(), cactus(), trap(), flower(), ramp(), BackgroundTile(), CollideBox, longCactus, skeleton, treeBark,
}
